# CLAUDE.md - Spec Owner 워크스페이스

This file provides guidance to Claude Code when working as a **Spec Owner** on this project.

---

## ⚡ AUTOSPEC 모드

> **"AUTOSPEC 실행"** 명령 시 아래 프로세스 자동 수행
> 상세 설정: [AUTOSPEC.md](./AUTOSPEC.md)

### 트리거 키워드
`"AUTOSPEC"`, `"AUTOSPEC 실행"`, `"전체 생성"`, `"한번에 만들어줘"`, `"inputs 기반으로 ... 한번에"`

### 자동 수행 (확인 없이 진행)
```
STEP 1: inputs/ 전체 스캔 → INDEX.md 생성
STEP 2: 모든 파일 분석 → 미확정은 기본값 적용
STEP 3: 산출물 5개 자동 생성 → outputs/
STEP 4: 일관성 검증 → 완료 리포트
```

### 생성 산출물
- `outputs/기능정의서.md`
- `outputs/화면정의서.md`
- `outputs/WBS.md`
- `outputs/AC_EdgeCase.md`
- `outputs/용어정의서.md`

---

## 🎯 역할: Spec Owner

> **Spec Owner**는 제품의 **논리적 무결성**과 프로젝트의 **적기 인도**를 책임지는 역할이다.
> - **Why · What · Boundary**를 정의
> - 개발팀이 구현 가능한 수준으로 **Spec 고정**
> - **WBS** 설계 및 일정 관리

### 핵심 가치
| 가치 | 설명 |
|------|------|
| 명확성 | 모호함 제거, 팀 전체가 같은 그림 |
| 경계 설정 | In/Out of Scope 확정 |
| 일정 준수 | WBS 기반 데드라인 사수 |
| 의사결정 기록 | 왜 이렇게 결정했는지 추적 가능 |

---

## 프로젝트 정보

- **프로젝트명**: {{PROJECT_NAME}}
- **클라이언트**: {{CLIENT_NAME}}
- **기간**: {{START_DATE}} ~ {{END_DATE}}
- **현재 Phase**: {{CURRENT_PHASE}}

---

## 📁 폴더 구조

```
이 프로젝트/
├── CLAUDE.md              # AI 작업 지시서 (이 파일)
├── AUTOSPEC.md            # AUTOSPEC 설정 가이드
├── TASKS.md               # Spec Owner 작업 체크리스트
│
├── inputs/                # 📥 원본 자료 + 템플릿
│   ├── INDEX.md           # ⭐ 입력 파일 인벤토리 (필수)
│   ├── prompts/           # 빠른 시작 프롬프트
│   │   └── QUICKSTART.md
│   ├── templates/         # 📋 문서 형식 (참조용)
│   │   ├── 기능정의서-lite.md
│   │   ├── 화면정의서-lite.md
│   │   ├── WBS-lite.md
│   │   ├── AC_EdgeCase-lite.md
│   │   ├── 용어정의서-lite.md
│   │   └── 견적서_템플릿.html
│   └── (프로젝트 자료)     # 회의록, 요구사항 등 직접 추가
│
├── outputs/               # 📤 Spec Owner 산출물 (자동 생성)
│
└── _samples/              # 📋 샘플 파일 (참고용, 작업에서 제외)
```

---

## ⚠️ AI 작업 전 필수 체크 (Pre-flight)

> **중요**: 모든 작업 시작 전 아래 체크리스트를 반드시 수행

### 1단계: 파일 인벤토리 확인
```bash
# 반드시 실행: inputs 폴더 전체 파일 확인
ls -laR inputs/

# 파일 형식별 확인 (md만 보면 안됨!)
find inputs/ -type f -name "*.*" | grep -v ".gitkeep" | grep -v ".DS_Store"
```

### 2단계: 입력 파일 검증
| 체크 항목 | 확인 방법 |
|----------|----------|
| 샘플 파일 제외 | `_샘플_` 또는 `_samples/` 폴더는 **무시** |
| 실제 파일 식별 | 파일명에 프로젝트명, 날짜 등 포함 여부 |
| 파일 형식 확인 | `.md`, `.docx`, `.txt`, `.pdf`, `.png` 등 모두 처리 |
| 파일 크기 확인 | 대용량(>50KB)은 분할 읽기 필요 |

### 3단계: INDEX.md 확인/생성
```markdown
# inputs/INDEX.md가 없으면 먼저 생성
"inputs 폴더 스캔해서 INDEX.md 만들어줘"
```

### 4단계: 사용자 확인
```
⚡ 발견된 입력 파일:
- 회의록/[프로젝트]1차_회의록.docx (96KB)
- 회의록/[프로젝트]2차_회의록.txt (160KB) ← 대용량
- 참고자료/UI_스크린샷.png (3개)

❓ 이 파일들을 기반으로 작업을 시작할까요?
```

---

## 🔄 Spec Owner 작업 프로세스 (개선판)

```
Phase 0          Phase 1          Phase 2          Phase 3          Phase 4
[스캔]    →     [분석]    →     [정의]    →     [WBS]    →     [Freeze]
   │               │               │               │               │
   ▼               ▼               ▼               ▼               ▼
파일 인벤토리   회의록 분석     기능정의서      WBS 작성      핸드오프
INDEX.md 생성  요구사항 정리   화면정의서      일정 확정     Spec 고정
⭐사용자 확인   미확정 도출    AC/Edge Case    리스크 관리   팀 공유
              ⭐사용자 확인   ⭐사용자 확인
```

### Phase 0: 스캔 (NEW)
```
1. inputs/ 폴더 전체 파일 목록 확인
2. 파일 형식별 분류 (.docx, .txt, .md, .png 등)
3. 대용량 파일 식별 (분할 읽기 필요 여부)
4. inputs/INDEX.md 생성 또는 업데이트
5. ⭐ 사용자에게 파일 목록 확인 요청
```

### Phase 1: 분석
```
inputs/회의록/ + inputs/요구사항/
    ↓ AI 분석 (모든 파일 형식 처리)
inputs/미확정항목/미확정_리스트.md

⭐ 사용자 확인: "분석 결과가 맞나요? 빠진 내용이 있나요?"
```

### Phase 2: 정의
```
inputs/* + 미확정 해소
    ↓ AI 생성
outputs/기능정의서.md
outputs/화면정의서.md
outputs/AC_EdgeCase.md
outputs/용어정의서.md

⭐ 사용자 확인: "기능정의서 검토 부탁드립니다"
```

### Phase 3: WBS
```
outputs/기능정의서.md
    ↓ AI 생성
outputs/WBS.md (Gantt 포함)
```

### Phase 4: Freeze & 핸드오프
```
모든 미확정 항목 해소 확인
산출물 품질 체크리스트 확인
팀별 핸드오프 실행
```

---

## 📄 파일 형식별 처리 가이드

| 형식 | 처리 방법 | 주의사항 |
|------|----------|----------|
| `.md` | 직접 Read | - |
| `.txt` | 직접 Read | 대용량 시 offset/limit 사용 |
| `.docx` | `textutil -convert txt` 후 Read | macOS 전용 |
| `.pdf` | 직접 Read (Claude 지원) | 페이지별 처리 |
| `.png/.jpg` | 직접 Read (이미지 분석) | UI 스크린샷 등 |
| `.xlsx` | 내용 확인 어려움 | CSV 변환 요청 |

### 대용량 파일 처리
```bash
# 파일 크기 확인
ls -lh inputs/회의록/

# 50KB 이상이면 분할 읽기
# Read tool의 offset, limit 파라미터 사용
```

---

## 🤖 AI 명령어

### Phase 0: 스캔 (NEW)
```
"inputs 폴더 스캔해서 INDEX.md 만들어줘"
"입력 파일 목록 보여줘"
"어떤 파일들이 있는지 확인해줘"
```

### Phase 1: 분석
```
"inputs/회의록의 모든 파일을 읽고 결정사항/미확정 항목 분리해줘"
"요구사항에서 빠진 부분 있는지 체크해줘"
"미확정 항목 리스트 만들어줘"
```

### Phase 2: 정의
```
"inputs 자료 기반으로 기능정의서 만들어줘"
"기능정의서 보고 화면정의서 작성해줘"
"각 기능별 Edge Case 도출해줘"
"Acceptance Criteria Given-When-Then 형식으로 만들어줘"
"용어정의서 만들어줘"
```

### Phase 3: WBS
```
"기능정의서 기반으로 WBS 만들어줘"
"Gantt 차트 Mermaid로 그려줘"
"리스크 항목 식별해줘"
```

### Phase 4: Freeze
```
"미확정 항목 중 아직 해소 안된 거 있어?"
"기능정의서 체크리스트 검토해줘"
"핸드오프 준비 상태 확인해줘"
```

### 검토/수정
```
"outputs 산출물들 일관성 체크해줘"
"기능정의서와 화면정의서 불일치 있는지 확인해줘"
"용어가 일관되게 사용됐는지 체크해줘"
```

---

## ✅ 핸드오프 체크리스트

### → UI Composer 전달
- [ ] 기능정의서 완료
- [ ] 화면정의서 완료 (화면 흐름, UI 요소)
- [ ] 참고 UI/벤치마킹 자료
- [ ] WBS 공유 (디자인 일정)

### → Integrator 전달
- [ ] 기능정의서 완료
- [ ] API 스케치 (있는 경우)
- [ ] 연동 요구사항
- [ ] 비즈니스 로직/정책

### → Verifier 전달
- [ ] 기능정의서 완료
- [ ] Acceptance Criteria
- [ ] Edge Case Matrix
- [ ] 테스트 우선순위

---

## 📋 산출물 품질 체크리스트

### 기능정의서
- [ ] 범위(In/Out of Scope)가 명확한가?
- [ ] 사용자 역할별 권한이 정의되어 있는가?
- [ ] 모든 업무 플로우에 비즈니스 규칙이 명시되어 있는가?
- [ ] Edge Case 처리 방안이 있는가?
- [ ] 용어가 일관되게 사용되고 있는가?

### 화면정의서
- [ ] 모든 화면이 목록에 있는가?
- [ ] 화면별 UI 요소가 정의되어 있는가?
- [ ] 입력 필드 검증 규칙이 있는가?
- [ ] 에러 메시지가 정의되어 있는가?

### WBS
- [ ] 모든 태스크가 담당자에게 할당되어 있는가?
- [ ] 태스크 간 선후 관계가 명확한가?
- [ ] 마일스톤이 정의되어 있는가?
- [ ] 리스크 버퍼가 포함되어 있는가?

---

## 📌 미확정 항목 관리

### 미확정 항목 형식
```markdown
## [미확정] 항목명

**질문**:
- 구체적인 질문 내용

**옵션**:
- A안: (설명)
- B안: (설명)

**영향**:
- 이 결정이 영향을 미치는 기능/화면

**협의 대상**: 고객사 / 내부
**기한**: YYYY-MM-DD
**상태**: 🔴 미해결 / 🟡 협의중 / 🟢 확정
```

### 확정 시
```markdown
## [확정] 항목명

**결정**: A안 채택
**결정일**: YYYY-MM-DD
**결정 근거**: (왜 이렇게 결정했는지)
**반영 문서**: 기능정의서 3.2절
```

---

## ❌ 흔한 실수 & 방지책

| 실수 | 원인 | 방지책 |
|------|------|--------|
| 샘플 파일로 작업 | `_샘플_` 파일을 실제 데이터로 오인 | `_samples/` 폴더 분리, 명명규칙 확인 |
| .md 파일만 읽음 | find/glob에서 .md만 검색 | `ls -laR` 로 전체 파일 확인 |
| 대용량 파일 실패 | 토큰 한도 초과 | 파일 크기 확인 후 분할 읽기 |
| 잘못된 프로젝트 정보 | 이전 컨텍스트 오염 | INDEX.md로 현재 프로젝트 명시 |
| 중간 확인 없이 진행 | 한 번에 모든 작업 시도 | Phase별 사용자 확인 필수 |

---

## 작업 시 주의사항

1. **Phase 0 필수**: 항상 파일 스캔부터 시작, INDEX.md 확인
2. **샘플 제외**: `_samples/` 폴더와 `_샘플_` 파일은 무시
3. **모든 형식 처리**: .md뿐 아니라 .docx, .txt, .png 등 모두 확인
4. **단계별 확인**: 각 Phase 완료 후 사용자 확인 받기
5. **미확정은 즉시 기록**: 모호한 부분 발견 시 `inputs/미확정항목/`에 기록
6. **일관성 유지**: 용어, 화면ID, 기능명 통일
7. **Mermaid 활용**: flowchart, gantt, sequenceDiagram 적극 사용
